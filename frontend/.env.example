# Base URL of the Pure Finance backend. In dev this points at the FastAPI
# service running via docker-compose or uvicorn on port 8000. Must include /api.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
